package io.github.some_example_name;

/**
 * Sets the default controls to be arrow keys (gets updated in the settings screen when the user selects)
 */
public class GameControlsConfig {
    // Default controls is Arrow Keys
    public static boolean useArrowKeys = true;
}
